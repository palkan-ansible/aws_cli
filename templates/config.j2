[default]
aws_access_key_id={{ aws_key }}
aws_secret_access_key={{ aws_secret }}
{% if defined?(aws_session_token) %}
aws_session_token={{ aws_session_token }}
{% endif %}
region={{ aws_region }}
{% if defined?(aws_output) %}
output={{ aws_output }}
{% endif %}